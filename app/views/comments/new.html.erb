<p><%= render @commentable %></p>

<%= form_for [@commentable, @comment] do |f| %>
  <div class="field">
    <%= f.label :content, 'Write a comment' %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
